server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stockflow?createDatabaseIfNotExist=true
    username: root
    password: "mysql password"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
